import React from 'react'; import { useTripStore } from '../../store/tripStore'; import { Trash2, Clock } from 'lucide-react';  export default function TimelineItem({ item }) {   const { setHoveredItem, deleteItineraryItem, hoveredItemId, selectedItemId } = useTripStore();    const isHighlighted = hoveredItemId === item.id || selectedItemId === item.id;    return (     <div       onMouseEnter={() => setHoveredItem(item.id)}       onMouseLeave={() => setHoveredItem(null)}       className={`relative p-3 rounded-xl border transition-all duration-300 group ${         isHighlighted           ? 'bg-indigo-950/40 border-indigo-500/50 shadow-md shadow-indigo-950/20'           : 'bg-slate-900/60 border-slate-800/80 hover:border-slate-700'       }`}     >       {/* Absolute Timeline Visual Indicator Ring */}       <div className={`absolute -left-[21px] top-4 w-2.5 h-2.5 rounded-full border-2 transition-transform duration-300 ${         isHighlighted ? 'bg-indigo-400 border-indigo-400 scale-125' : 'bg-slate-950 border-slate-700'       }`} />        <div className="flex justify-between items-start gap-4">         <div>           <div className="flex items-center gap-1.5 text-[10px] text-slate-400 font-medium mb-1">             <Clock className="w-3 h-3 text-slate-500" />             {item.time}             <span className="mx-1">•</span>             <span className="text-indigo-400">{item.category}</span>           </div>           <h4 className="text-xs font-bold text-slate-200">{item.title}</h4>         </div>          <div className="flex items-center gap-3 native-actions">           <span className="text-xs font-bold text-emerald-400">${item.cost}</span>           <button             onClick={() => deleteItineraryItem(item.id)}             className="text-slate-500 hover:text-rose-400 transition-colors opacity-0 group-hover:opacity-100"           >             <Trash2 className="w-3.5 h-3.5" />           </button>         </div>       </div>     </div>   ); }